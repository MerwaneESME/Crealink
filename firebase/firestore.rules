rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Règles temporairement plus permissives pour le débogage
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 